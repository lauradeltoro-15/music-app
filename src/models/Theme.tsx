export interface Theme {
  colors: {
    primary: string;
    secondary: string;
    primaryBackground: string;
    secondaryBackground: string;
  };
  fontSizes: {
    sm: string;
    md: string;
    lg: string;
  };
  fontFamily: string;
}
